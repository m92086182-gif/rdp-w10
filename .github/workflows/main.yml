name: RDP-Server
on:
  workflow_dispatch:

jobs:
  winserver-rdp:
    runs-on: windows-latest

    steps:

      - name: Enable RDP on Windows Server
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall set rule group="remote desktop" new enable=Yes
          Restart-Service TermService -Force

      - name: Create Static RDP User
        run: |
          $username = "AdminServer"
          $password = "Admin@12345"
          $secure = ConvertTo-SecureString -String $password -AsPlainText -Force
          
          if (-not (Get-LocalUser -Name $username -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name $username -Password $secure -AccountNeverExpires
          }

          Add-LocalGroupMember -Group "Administrators" -Member $username
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username
          
          echo "RDP_USER=$username" >> $env:GITHUB_ENV
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $file = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet /norestart" -Wait
          Remove-Item $file -Force

      - name: Connect Tailscale
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey "tskey-your-key-here" --hostname server-${{ github.run_id }}

          $ip = $null; $tries = 0
          while (-not $ip -and $tries -lt 10) {
              $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $tries++
          }

          if (-not $ip) { Write-Error "No Tailscale IP"; exit 1 }
          echo "TS_IP=$ip" >> $env:GITHUB_ENV

      - name: Show Info
        run: |
          Write-Host "==========================="
          Write-Host " RDP IP: $env:TS_IP"
          Write-Host " USER:   $env:RDP_USER"
          Write-Host " PASS:   $env:RDP_PASS"
          Write-Host "==========================="

      - name: Keep Alive
        run: |
          while ($true) {
              Write-Host "[$(Get-Date)] Active"
              Start-Sleep -Seconds 300
          }
